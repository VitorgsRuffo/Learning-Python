# We can get access to functionality defined
# in another file (module) by importing it
# into our current one.

import tools

print(tools.say_hi())
print(tools.square_sum([10, 1, 1]))
print(tools.var3)

# There are built-in and external modules.
#
# Some good external modules are installed
# when we install python.
#
# But if you want to access functionality
# from another module you can install it
# using pip.

# Pip is a package manager that you can use
# to install, update, uninstall, etc, python
# modules.
